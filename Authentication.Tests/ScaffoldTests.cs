namespace Authentication.Tests;

/// <summary>
/// Temporary scaffold check: proves the xUnit harness builds and runs under the
/// repo's analyzer + warnings-as-errors settings. The library has no public types
/// until milestone 2, so there is nothing else to assert yet. Delete this once
/// ReusableAuthOptions lands and brings real coverage with it.
/// </summary>
public class ScaffoldTests
{
    [Fact]
    public void TestHarness_Runs()
    {
        Assert.True(true);
    }
}
